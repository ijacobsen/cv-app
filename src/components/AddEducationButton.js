import React from 'react';

const AddEducationButton = (props) => {

    return (
        <button type='button' onClick={props.onAdd}> Add Education </button>
    )
}

export default AddEducationButton;